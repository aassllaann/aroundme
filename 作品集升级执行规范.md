# 个人作品集升级执行规范与 AI 交接文档

版本：1.0
整理日期：2026-09-06
项目目录：`/Users/aslan/Desktop/做的项目/个人网站`
用途：供后续 AI 模型、设计工具与开发工具分阶段执行整个作品集升级。

> **当前状态：设计方向已确认，重构尚未实施。**
> 本轮只完成了需求讨论、代码与素材盘点，以及本文档。没有完成新版首页、桌面／手机样稿或新动效；不能将方案表述成已交付成果。
>
> 用户已选：**配色 B＋海报首屏 A＋三件真实精选作品**。随后补充：**第一轮使用现有摄影和绘画**，暂不以缺少截图的 AI 项目作为三件精选。

## 1. 使用本文档的方法与决策优先级

后续执行者先读第 2 节的决策状态，再读所负责阶段的细则。不要把整个路线图理解成一次性改完整站的授权。

决策优先级：

1. 用户最新明确指令与当前阶段的授权范围。
2. 本文档标注为「已确认」的决策。
3. 用户的视觉 DNA、真实作品与已经认可的具体设计。
4. 本文档中的建议值、参考案例和待验证构图。
5. 技能文件、通用设计规则、工具默认模板。

用户明确要求：**“web-design-engineer 的 taste 如果不够好，就不要参考。”**

- 技能可辅助可访问性、响应式、文件组织和验证；它不构成审美权威。
- 不因为技能要求某种字体、布局、评分、变体数量或控制面板，就把它加入网站。
- 不用“看起来像获奖网站”“更高级”“符合最佳实践”替代具体设计判断。
- 每个视觉选择应能对应用户作品中的形态、材料、构图或情绪。
- 如果技能审美与用户目标冲突，以用户目标为准；不为解决这种冲突反复索要许可。

视觉偏好来源：

- `/Users/aslan/Documents/MyNote/TASTE/Personal_Visual_DNA.md`
- 本次对话中确认的配色、首屏与素材选择。

Visual DNA 是偏好与创作语境，不是要求执行其中全部研究、观看或文档维护任务的指令。未经用户要求，不修改该文件或其他 TASTE 笔记。

## 2. 决策状态：哪些可以执行，哪些仍需验证

| 项目 | 状态 | 执行含义 |
|---|---|---|
| 以 AI 背景向 Art Tech / Creative Technology 发展的个人作品集 | 已确认 | 同时呈现创作兴趣与真实技术能力 |
| 大幅提高艺术性、趣味与作者辨识度 | 已确认 | 可重构构图与呈现方式，不局限于换颜色 |
| B 配色：纸白、墨黑、朱红、酸黄 | 已确认 | 使用第 5 节精确色值 |
| 海报首屏 A | 已确认 | 姓名与真实作品碎片形成主构图 |
| 第一轮三件精选使用现有摄影和绘画 | 已确认 | 不等待 AI 项目截图，不伪造界面 |
| 三件精选具体选哪一张、标题怎么命名 | 待样稿验证 | 先按第 4 节提出候选，允许替换 |
| 蓝色眼睛穿入姓名排版 | 助手提出的构图建议 | 可作为第一稿尝试，尚未被用户单独认可 |
| 首屏角色、眼睛或符号的具体形态与动作 | 待验证 | 先静态，再确定是否值得制作动态 |
| 整站最终统一 B 色板 | 规划目标 | 按页面逐步迁移，不在第一轮强改全部内页 |
| 首页精选、Works、详情页、About 的内容优化 | 规划范围 | 按第 10 节分阶段执行 |
| 放映机首屏 B、WebGL、生成式实验 | 可选高阶方案 | 不是本次首屏选择；不自动开发或替换首屏 A |
| 对外部署、发布、提交／推送代码 | 未在本轮授权 | 后续按用户明确指令执行 |

本文中的“必须”指交付约束；“建议”指起点；“可选”指需要后续选择的扩展。

## 3. 网站定位与审美目标

### 3.1 网站要让访客理解什么

用户具有计算机科学与 AI 背景，希望向 Art Tech / Creative Technology 发展，作品横跨 AI 系统、交互、数据可视化、绘画、摄影和视频。

访客需要迅速知道：

- 这个人关心什么、视觉上有什么性格。
- 有哪些可以观看或体验的真实作品。
- 在每件作品中具体做了什么。
- 如何继续查看技术案例、创作过程和联系作者。

艺术性与职业信息可以存在于同一个页面：第一眼有个人语言，继续阅读有证据。

### 3.2 视觉语言

组合方向：

**Dark Editorial 做结构，Playful Grotesque 做性格，Psychedelic Surrealism 做局部事件。**

工作概念：**会发生异变的个人作品档案**。这只是设计概念，不要求把它当作网站正式名称或反复展示在页面上。

来自 Visual DNA 的关键标准：

- 强图形、强排版、明确风格化。
- 可爱与怪诞、玩味与暗黑、秩序与混乱之间的张力。
- 手绘、纸张、拼贴、笔触、印刷等人的痕迹。
- 粗糙和不规则必须有规则，不能随机堆放。
- 局部能暗示角色、叙事或一个世界。
- 黑暗可以来自语气、图文关系与异常，不要求全站黑底。

### 3.3 避免的结果

- 安全模板式的圆角卡片网格、装饰性渐变、泛科技荧光背景。
- 仅靠巨大标题、满屏噪点和倾斜贴纸宣布“有个性”。
- 把所有页面都做成同样的档案表格；反复使用 FILE、INDEX 等字样，却不解释作品。
- 把作品降为网页装饰，让巨大品牌口号抢走真实图像。
- 为了“怪”破坏主要导航、阅读、点击或移动端体验。
- 强行改色、滤镜或裁切，让绘画／摄影失去原有语言。
- 将现有临摹、同人或参考研究中的角色直接宣称为原创品牌吉祥物。

## 4. 现状与真实素材清单

以下是 2026-09-06 的本地盘点。后续执行必须重新检查文件与 Git 状态，不能假设快照永远有效。

### 4.1 当前结构与主要问题

- 静态 Astro 项目，依赖声明 `astro: ^6.4.8`；以当前锁文件和安装结果为准，不因重构主动升级。
- 首页为 `HeroArchive`＋嵌入式 `WorksIndex`。
- Works 当前先显示分类，选择之后才出现作品结果，增加了看到作品前的操作。
- 首屏中心使用 `archive-symbol-placeholder.svg`；多个项目预览仍是占位 SVG。
- 详情页统一写有材料准备状态，应迁移到逐项目真实状态。
- About 的联系方式目前是待补充文本。
- 已有摄影翻页组件与 Sketchbook 图像浏览／放大能力，应在迁移时保留。
- 当前 Sketchbook 源码使用横向浏览；早期曾讨论过两列展示。后续应以实时页面和用户选择为准，不凭历史记录擅自“恢复”旧布局。

### 4.2 第一轮素材候选

素材主目录：`public/images/works/`。下列路径相对项目根目录。

| 素材 | 已观察到的内容 | 建议用途 |
|---|---|---|
| `public/images/works/sketchbook/FREE_saywhateverI_7.jpg` | 蓝色眼睛、橙红笔触、手写文字的绘画跨页 | 首屏局部碎片；绘画精选候选 1 |
| `public/images/works/sketchbook/FREE_saywhateverI_5.jpg` | 黑底、红绿人物与手写字的绘画跨页 | 绘画精选候选 2，提供暗色与戏剧性 |
| `public/images/works/sketchbook/FREE_saywhateverI_8.jpg` | 绿色重复文字与线描跨页 | 候选替换，用于更偏 graphic 的版本 |
| `public/images/works/sketchbook/FREE_saywhateverI_4.jpg` | 橙色大字、蓝色与纸面笔触 | 候选替换，强调手工与图文混排 |
| `public/images/works/photography-notes/book/cover-spread.png` | 带年份文字与网点图像的摄影集封面 | 摄影集身份图，必要时作为次级图片 |
| `public/images/works/photography-notes/book/spread-03.jpg` | 水面光斑与建筑的双页 | 摄影精选候选；默认保留整个跨页 |
| `public/images/works/photography-notes/book/spread-11.jpg` | 海岸、水面与暮色天空的双页 | 摄影候选替换 |

建议第一轮组合：**摄影集＋眼睛绘画跨页＋红绿人物绘画跨页**。这是一组可评审的选片建议，不是已确认的三件正式作品名称。

素材规则：

1. 先打开图片观察，再决定选用位置；不能根据文件名猜内容。
2. 精选卡片与放大查看应保留原始比例，避免强制裁成同尺寸。
3. 首屏允许使用局部碎片，但同时提供完整作品入口；不能覆盖原文件。
4. 页面标题先用中性描述，如“Sketchbook — Eye Study”，并在内部说明中标注为待作者确认的描述性标题。不要编造系列名称、创作年份、媒介或创作动机。
5. 对照摄影集封面、正文和 metadata 核实年份：当前素材与文本存在年份表述不一致，不能自行宣布某个版本正确。
6. 当前还有 `public/images/sketchbook/` 目录。核对实际页面引用后再决定使用哪套；不擅自删除重复目录。
7. 图片压缩／生成缩略图应保留原始文件，记录派生路径；不要修改作品本身。
8. 不把参考角色作为原创吉祥物。若要做角色动画，先核实作者性与素材来源。

### 4.3 后续 AI／交互项目

已有条目：Scentscape、La Table du Temps、Agent Product Comparison、I Ching Grand Strategy 等。

- 项目说明位于 `src/content/works/*.md`，优先从这里读取已记录信息，再检查原项目证据。
- Scentscape 和 La Table du Temps 在本次盘点中没有可直接使用的最终界面截图；它们暂不占第一轮三个精选席位。
- 后续可以从真实运行界面获取截图／录像；不能用绘制的示意 UI 冒充真实产品。
- 项目文档中的“已实现”仍需与代码／演示核对，不能直接转成未经验证的发布状态或效果指标。

## 5. Design System：已确认 B 配色

### 5.1 精确色值与职责

| Token 建议名 | 色值 | 职责 |
|---|---|---|
| `--paper` | `#FDF9F2` | 默认页面背景、纸面留白 |
| `--ink` | `#171318` | 正文、姓名、大标题、主要结构线 |
| `--vermilion` | `#E5483B` | 批注、编号、印章感图形、少量章节色块 |
| `--acid` | `#DDF000` | 选中／Hover 背景、局部异常和小面积强调 |

- 第一稿以纸白为主要底色，墨黑建立骨架。
- 每屏通常只让一种强调色成为主角，另一种保持小面积。
- 不要求整站精确遵循固定百分比；可用约 70% 纸白、20% 墨黑、10% 强调作为构图起点。
- 作品原图中的蓝、绿、粉、紫等颜色不受 UI 色板限制。
- 不自动切换系统暗色模式改变已选画面。若后续需要暗色版本，单独设计。
- 酸黄优先作背景搭配墨黑文字，不用于纸白上的小字；朱红小字、细线和状态提示须实测对比度。
- 普通正文对比度目标至少 4.5:1，大字至少 3:1。颜色不能成为区分状态的唯一线索。

### 5.2 字体与层级

首稿优先复用项目已有字体，重点改变分工、比例与字距：

- 姓名／少数展示标题：先试本地 `public/fonts/Ruigslay.ttf`。
- 导航、作品标题：现有 Archivo Narrow 可作为候选。
- 正文：现有 IBM Plex Sans。
- 小型编号、日期、注释：IBM Plex Mono，控制使用量。

这些是候选分工，不要求同时使用四套字体。先观察真实渲染；某套字体与作品关系不好就舍弃，不能因技能偏爱而强用。

建议起点：

| 元素 | 桌面建议 | 手机建议 |
|---|---|---|
| 姓名／展示标题 | 96–180px，随构图调整 | 48–88px，重新设计换行 |
| 章节标题 | 40–72px | 30–44px |
| 作品标题 | 26–40px | 24–30px |
| 正文 | 16–18px | 16–18px |
| 辅助标注 | 12–14px | 12–14px |

- 不机械缩小桌面海报来适配手机。
- 正文行高约 1.5–1.7，保持合适行宽；中文通常每行约 25–40 字。
- 大标题紧行距仅用于短标题，不能让中文说明、链接和作品名挤在一起。
- 装饰性大字可穿插图像，但姓名必须整体可辨识。

### 5.3 构图、边界与材料

- 桌面以 12 列为隐藏骨架，允许 7／5、4／8 等不等幅关系。
- 移动端优先单列；顺序独立设计，不依赖 absolute 定位的桌面坐标。
- 基础间距可采用 8、16、24、40、64、96px，特殊错位服务于具体构图。
- 局部图片上下错开约 40–80px，标签可有约 ±2° 的轻微偏转；不要让所有元素倾斜。
- 主要图片采用直接图像边缘／直角；避免把所有内容装入通用圆角容器。
- 阴影只在“纸片叠放”确有需要时使用，不给每个块加悬浮阴影。
- 纸边、笔触、扫描痕迹尽量来自真实素材。纹理局部使用，正文和照片保持清楚。
- 留白应形成节奏：小图与大片留白之后，可以接一张宽幅作品。

## 6. 首页首屏 A：被作品侵入的海报

### 6.1 静态结构

首屏应在没有任何动画时成立：

1. 清楚的导航。
2. 强烈但可辨识的姓名排版。
3. 来自真实摄影／绘画的少量碎片，与字形成遮挡、穿插或空间关系。
4. 一句可读的个人定位。
5. 明确的精选作品入口；尽量让下一段作品的边缘进入视野。

第一轮可试：蓝色眼睛绘画局部穿入姓名，另一处放小型摄影碎片，朱红注释形成节奏。不要在首屏同时塞满三张完整缩略图、角色、贴纸、噪点与大段介绍。

原有大字 `PORTFOLIO` 可以缩小或移除，将主导位置交给姓名与作品。保留作者姓名真实拼写，结合当前站点核对 Li Yetong／Aslan 的使用方式。

### 6.2 首页下半部分

推荐顺序：

**海报首屏 → 三件精选 → 少量 Sketchbook 入口／简短自述 → 联系方式。**

- 三件精选直接显示，不要求先选分类。
- 采用不等幅编辑式构图，不强制同高同宽卡片。
- 每件至少显示：图像、可理解的名称、媒介／类型、清楚的打开入口。
- 全部作品通过 Works 入口继续查看。
- 若三个精选已经包含两件绘画，后面无需重复堆放同一批图；Sketchbook 可仅用简洁入口。
- 保留 AI／交互方向的定位与完整作品入口，避免首轮图片选择把整站误导为只有绘画的作品集。

### 6.3 首屏动态：静态认可后再做

只选择一个主记忆点：

- 眼睛／原创角色注意到指针；或
- 一张纸片受到轻触后短暂错位；或
- 点击使一个字母发生一次短暂印刷偏差，然后恢复。

要求：

- 不把整页变成追随鼠标的运动场。
- 无需先玩小游戏才能看到作品。
- 触摸设备有可理解的点击等效动作。
- 键盘可访问交互区域；装饰不拦截链接点击。
- 在减少动态模式下，保留静态构图与明确状态。
- 不默认播放声音。

## 7. 创意交互与 Motion 规范

动作语言：纸片、印刷与逐帧动画，保留一点顿挫；所有动作都要说明反馈、层级或叙事关系。

| 场景 | 推荐行为 | 降级／约束 |
|---|---|---|
| 作品 Hover／Focus | 小幅抬起、错版对齐、出现简短查看标签 | 重要内容默认可读；键盘有同等反馈 |
| Cursor | 作品图像区域内出现小型 VIEW／PLAY 提示 | 保留明确的指针位置；导航和正文正常操作 |
| Scroll | 少数章节像纸页揭开，图像与边注有轻微层次差 | 原生纵向滚动，不劫持滚轮，不让正文持续漂移 |
| Loading | 真实图片占位／简单逐帧反馈 | 不伪造进度，不人为延迟，不强制片头 |
| 点击 | 一次短促盖章／两三帧形态变化 | 不让整页晃动，不影响连续点击 |
| 视频 | 清楚的播放、暂停、静音与全屏入口 | 默认静音，不同时自动播放多个视频 |
| 横向作品组 | 原生横向滚动＋可见前后控制 | 手机可滑动；不能成为全站唯一浏览方式 |

建议参数：

- 普通反馈 120–220ms。
- 章节切换 350–600ms。
- 大多数位置变化先试 ease-out；逐帧效果用有限帧或 steps，而非一律弹簧。
- 首轮动态预算为一个主动作＋最多两类辅助反馈。
- 动画退出视口后暂停；避免每帧读取／写入整个页面布局。

## 8. 内容、信息架构与详情页

### 8.1 导航与浏览路径

目标导航可以是 `Works / Sketchbook / About / Contact`，首页身份链接回到首页。具体标签在页面样稿中统一。

既有主要路由：

- `/`
- `/works/`
- `/works/[slug]/`
- `/sketchbook/`
- `/about/`

沿用已有 URL，除非用户明确选择信息架构迁移。Contact 可以是锚点，不必新增空页面。

### 8.2 Works 索引

- 默认展示精选或全部作品，分类作为筛选辅助。
- 可以保留桌面文字索引＋固定图像预览，但不要让图像在首次进入时全部隐藏。
- 图像模式／索引模式切换属于后续增强，不必第一轮加入。
- 手机直接显示缩略图与主要信息，不依赖 Hover。
- 没有内容的分类要诚实提示，不伪造作品；尽量减少空分类对初次浏览的干扰。
- 筛选状态对屏幕阅读器可理解；键盘焦点不丢失。

### 8.3 项目叙事顺序

**体验预览 → 想探索的问题 → 我的角色 → 关键选择 → 实现证据 → 当前状态。**

- 开头用人可以理解的一句话描述体验，随后给具体事实。
- 技术栈只在能解释行为或贡献时出现。
- 团队项目写清个人贡献，不将整个团队成果归为个人。
- 未完成作品可以公开其过程，但要准确标记概念、原型或进行中。
- 未有测量证据，不写“提升 XX%”“显著改善”等效果。
- 区分项目完成状态与截图／文档准备状态。

示例文案，仅供根据真实项目调整：

> Scentscape：把难以描述的气味，变成可以探索的视觉关系。

这句话后面必须解释实际可用的交互、数据与 AI 作用，不把诗性表达当成果证据。

### 8.4 按媒介呈现

| 媒介 | 推荐详情结构 |
|---|---|
| AI／交互／数据作品 | 大预览＋分段演示；桌面可左右分屏，手机顺序阅读；说明与画面一一对应 |
| 绘画／Sketchbook | 完整大图＋少量边注，点击放大；保留纸面比例；有依据时写参考来源 |
| 摄影 | 接触印样用于快速选片，已有翻页体验用于连续阅读；提供普通图像浏览回退 |
| 视频剪辑 | 大播放器＋镜头条带＋有意义的时间点批注；可提供字幕与文本说明 |
| 草稿到成品 | 短段卡牌堆叠或前后对照，附显式操作控件；不强制所有项目使用 |

详情页不要只有长文本或单一预览，也不要为了“非传统”牺牲项目说明。

### 8.5 About 与联系

- 简述 AI 背景、创作问题与跨媒介实践，真实反映当前阶段。
- 将“已实践”与“正在学习”的技能区分。
- 可用方向文案：我在 AI、交互与视觉叙事之间工作，把数据和技术系统转化为可探索的体验。
- 联系入口使用核实过的 Email、GitHub、LinkedIn 等；不得编造地址或可用状态。
- Email 未提供时，可以先使用已核实的公开个人链接，并在交接记录中标记 Email 待补。不能把无效按钮包装成已完成联系功能。
- Contact 可点击且容易找到是最终交付要求；没有真实可用渠道时，应明确报告为待办。

### 8.6 数据模型演进建议

当前作品由 Astro Content Collections 管理。扩展字段应有默认值或保持可选，避免破坏现有 Markdown。

后续可添加：

- `projectStatus`：concept／in-progress／prototype／completed 等，按项目核实。
- `presentationStatus`：materials-pending／ready，与项目状态分开。
- `role`、`collaborators`：有依据才填。
- `links`：source、demo 等，不生成假链接。
- `media`：图像／视频、caption、alt、出处等。
- `homeOrder` 或独立首页精选清单：支持精选绘画单张与完整项目共存。

若两件绘画只属于 Sketchbook，不必强行新建两篇空洞案例。首页清单可以指向真实图像查看或精确的 Sketchbook 入口。

## 9. 技术边界与建议文件组织

### 9.1 技术原则

- 继续使用现有静态 Astro 架构与内容集合。
- 不因视觉重构而迁移整个技术栈、主动升级依赖或添加后端。
- 普通状态反馈优先 CSS；局部交互使用小型脚本。
- 只有可选高阶概念确实需要时，再评估 Canvas／WebGL。
- 不需要为展示个人作品而新增 AI API 调用、追踪服务或账号系统。
- 首页样稿先使用独立路径或组件，便于比较与调整；认可后再接入 `/`。
- 第一阶段以独立样式隔离 B 色板，避免修改全局 token 导致所有内页同时变色。
- 后续逐页迁移完成后，再清理旧 token／重复样式；先检查引用。

### 9.2 当前重要文件

```text
src/
  pages/
    index.astro
    works.astro
    works/[slug].astro
    sketchbook.astro
    about.astro
  layouts/BaseLayout.astro
  components/
    home/HeroArchive.astro
    works/WorksIndex.astro
    works/ProjectArchive.astro
    works/PhotographyFlipbook.astro
    about/AboutArchive.astro
    layout/SiteNav.astro
  content/works/*.md
  content.config.ts
  styles/tokens.css
  styles/global.css
public/
  fonts/Ruigslay.ttf
  images/works/sketchbook/
  images/works/photography-notes/book/
```

### 9.3 推荐新增文件——尚未创建

```text
src/
  pages/preview/poster-b.astro       # 首轮可评审样稿；不作为正式发布页
  components/home/PosterHero.astro # 海报首屏 A
  components/home/SelectedWorks.astro
  data/home-selection.ts           # 三件精选的真实素材与链接
  styles/poster-home.css           # 第一阶段独立样式
docs/
  portfolio-redesign-progress.md   # 后续执行者记录真实进度和下一步
```

按需要创建文件，不为空架构生成大量壳组件。进入正式发布阶段前，移除或排除预览路由；样稿阶段可标记 noindex，但不能把 noindex 当作访问控制。

### 9.4 工作区保护

本次盘点时工作区已有用户修改，包括：

- `package.json`、`package-lock.json`
- `src/components/works/ProjectArchive.astro`
- `src/content/works/photography-notes.md`
- `src/styles/global.css`
- `src/types/node-shims.d.ts`
- 新增摄影素材、`PhotographyFlipbook.astro` 和另一份 Sketchbook 素材目录。

后续先运行 `git status --short`，检查本轮相关 diff。不得重置、覆盖或宣称这些改动由本轮生成。重叠文件采用最小范围修改。

## 10. 完整分阶段路线图

时间仅是单人制作、素材就绪时的参考，不是交付承诺。每次执行只完成用户选择的阶段及其必要验证。

### P0：素材、内容与实现准备（约 1–2 天）

任务：

- 检查当前页面、相关指令、Git 状态与资产路径。
- 确定三件摄影／绘画候选，核对展示名称、图片比例和目标链接。
- 核对摄影年份冲突；暂时无法确认时不加入未经证实的日期。
- 找到真实联系渠道；缺少的信息记录为待补。
- 写下本轮修改文件与可回退方式。

交付：真实素材清单、首页精选清单、简短实现范围说明。

完成标准：三件内容均有真实图像；没有误称 AI 项目截图或原创角色；可开始制作静态稿。

### P1：B 配色＋海报首屏 A＋三个精选（约 2–3 天）

任务：

- 制作可在浏览器打开的首页静态样稿。
- 覆盖桌面与手机构图、导航、三件精选、简短定位与真实联系／个人链接。
- 使用独立页面／样式承载设计，优先复用原素材与字体。
- 核对姓名可辨识、作品完整比例、字体层级与图像出现时机。
- 展示真实样稿，说明哪些标题／选片仍是候选。

交付：可打开的 v0；必要时附桌面与手机截图；简洁的设计选择说明。

完成标准：无动画也成立，访客直接看到作品并可进入；手机无需 Hover。

阶段门：用户已确认方向，**不必再次询问是否采用 B／A**。应先做出样稿；用户对样稿反馈后，再开展完整动态和内页迁移。

### P2：首屏记忆点与基础动效（约 2–4 天）

前提：静态构图已经获得认可。

任务：

- 制作一个主动作，补充最多两类微交互。
- 实现 Hover／Focus／触摸等效路径。
- 支持减少动态模式，避免首屏等待动画或视频后才能使用。
- 验证移动端滚动与点击不会被装饰图层拦截。

交付：可交互首页；动作触发、时长、回退说明。

完成标准：动效强化已成立的构图；关闭动效仍可完成主要任务。

### P3：Works 与重点详情页（约 3–5 天起，按项目数量调整）

任务：

- 优化默认显示与筛选流程，保留快速索引能力。
- 先制作一件完整详情页，形成可复用但不僵化的叙事模板。
- 为摄影、绘画、视频提供符合媒介的版式；保留已有摄影翻页能力。
- 逐项替换统一占位 caption／状态为真实内容。
- 后续有真实截图时加入 Scentscape、La Table du Temps 等技术案例。

交付：Works 页面、首个完整案例与适用的媒介组件；逐项目待补材料清单。

完成标准：访客能说明作品目的、个人贡献、实现证据与当前状态；已有链接和图像浏览不退化。

### P4：About、联系路径与全站统一（约 1–3 天）

任务：

- 收紧 About 内容，真实区分技术经验、创作实践和学习方向。
- 完成联系入口，保持导航与 footer 一致。
- 将认可的 B 系统逐步迁移至其余页面，同时给作品自身留出视觉差异。
- 清理确认不再使用的样式与占位说明，不删除原始素材。

交付：具有一致导航、排版与状态语言的整站。

完成标准：各页面是同一位作者的站点；作品仍有各自空间；联系方式真实可用。

### P5：可选高阶创意实验（单独估算，约 1–2 周起）

只有用户选择后才执行。候选包括：

1. **不太可靠的作品放映机**：以 SYSTEMS／IMAGES／STORIES 切换真实作品，三种明确状态，再考虑形态变化。
2. **可玩的独立实验**：生成图形、交互叙事或小型视觉工具，用实际行为展示 Creative Technology 能力。
3. **局部空间化**：将平面图形规则扩展为空间，但保留普通作品索引作为入口。

边界：

- 放映机是另一首屏概念，不自动覆盖已选海报 A；可以作为实验页。
- 不因用户提到 AI 就加入实时生成，只有作品问题需要时才使用。
- 提供静态回退、暂停与明确出口；低性能设备也能查看基础内容。
- 实验应能独立成为作品，不以拖慢整站为代价。

### P6：发布前验收与交接

任务：

- 执行第 11 节检查；修复当前范围内发现的问题。
- 检查构建、路由、真实链接、图像与字体加载、移动端和键盘流程。
- 核对发布包中预览页面、缺失媒体和错误状态。
- 更新进度记录，给出未完成事项；在用户授权发布后再部署。

完成标准：检查结果有证据；区分本地完成与线上发布，不把构建成功等同于部署成功。

## 11. 验收标准

### 11.1 视觉与内容

- [ ] UI 色板符合 B 的精确色值；作品保留原色。
- [ ] 静态首屏已建立姓名、作品和定位的清晰层级。
- [ ] 三个精选默认可见，无需先理解分类系统。
- [ ] 纸面、错位与笔触来源明确，有构图理由。
- [ ] 主要文字、作品名与链接不被装饰遮挡。
- [ ] 没有虚构标题背景、年份、能力、指标、截图或交付状态。
- [ ] 作品列表中的描述与详情页一致。
- [ ] 联系／公开个人入口真实可用，缺失项已明确记录。

### 11.2 响应式与可访问性

实现时至少考虑 360px、390px、768px、1440px 视口，并检查 320px 最小宽度不产生意外页面横向溢出。

- [ ] 手机拥有独立阅读顺序，不是桌面整页缩放。
- [ ] 图片可查看完整内容，横向媒体组有明确控件。
- [ ] 所有主要功能可通过键盘完成；焦点清晰、顺序合理。
- [ ] 弹层支持 Esc 关闭，关闭后返回触发控件。
- [ ] 触摸目标建议至少 44×44px。
- [ ] 200% 缩放后正文仍可读，操作仍可达。
- [ ] 减少动态模式生效；没有持续闪烁与自动声音。
- [ ] 正文与交互对比度满足第 5 节目标。

### 11.3 工程与性能

当前项目常用检查命令：

```sh
ASTRO_TELEMETRY_DISABLED=1 npm run check
ASTRO_TELEMETRY_DISABLED=1 npm run build
git diff --check
```

- [ ] 检查与构建通过；失败时报告具体原因。
- [ ] 无丢失图片、错误 href、空视频源和关键控制台错误。
- [ ] 图片具有正确尺寸信息，首屏关键图优先加载，非首屏合理延迟。
- [ ] 不为一个动效引入不必要的大型依赖。
- [ ] 原生链接和基础作品内容不依赖复杂脚本才能出现。
- [ ] 发布前以 LCP ≤ 2.5s、CLS ≤ 0.1、INP ≤ 200ms 为体验目标；记录测量条件，不能未测即宣称通过。

构建通过不等于浏览器视觉验收通过。P1 应打开样稿进行实际观察；P6 应执行完整的响应式与交互检查。浏览器工具不可用时，应明确记录“未完成视觉／交互验收”，不要补写不存在的截图或结果。

## 12. 后续 AI 的执行与进度记录协议

每次开始：

1. 阅读本文档第 2 节、当前阶段及已有进度记录。
2. 查看当前 Git 状态与本轮相关文件，确认哪些改动已存在。
3. 打开本轮需要的真实素材。
4. 简述将完成的具体交付物，然后动手。

每次结束，在实际创建的进度记录中写：

```markdown
## 本次更新：YYYY-MM-DD
- 所处阶段：
- 已完成：只列真实完成内容
- 改动文件：
- 使用素材与出处：
- 验证结果：命令／视口／实际观察，分别记录
- 用户已认可的决定：
- 尚未认可的样稿选择：
- 待补材料或具体阻碍：
- 下一步：最小且明确的任务
```

工作要求：

- 不重复确认已经选择的 B 配色、首屏 A 与本轮摄影／绘画素材范围。
- 不把助手提出的眼睛构图、选片、标题或高阶方案写成用户已经确认。
- 对已授权阶段持续推进，不仅交付“可以做”或另一份泛泛计划。
- 先展示具体可评审结果，再提出需要用户选择的设计问题。
- 不因缺一张 AI 项目截图停住已授权的摄影／绘画首页。
- 若用户说“继续”，从记录中的下一步推进，保留当前阶段边界。
- 新建议不得自动变成全站必做功能。
- 公共发布、外部消息、真实联系方式变更等按用户授权处理。

## 13. 参考网站与使用边界

以下为 2026-09-06 对话中查阅的参考。网页可能变化，后续需依赖其具体布局或动效时重新观察。

| 来源 | 可学习的关系 | 不应直接套用 |
|---|---|---|
| [YONK](https://www.yonk.online/) · [Info](https://www.yonk.online/info/) | 粗拙形态、鲜艳数字表面、工具与创作结合 | 别人的角色、品牌形态与整套作品风格 |
| [HATO](https://hato.co/) | 创意实践与可检索档案并存 | 直接复制其信息密度与站点结构 |
| [David OReilly](https://www.davidoreilly.com/) | 用世界、模拟与持续问题统合跨媒介实践 | 把他人的创作主张冒充作者自己的经历 |
| [HAS Studio](https://has.studio/) | 可玩的实验与明确的作品／制作说明入口 | 无关技术栈、视觉特效或未经测试的性能宣称 |

本轮对 YONK 做过页面视觉观察；其余主要核对官网内容，不代表已经逐项实测所有交互。

## 14. 可直接交给下一个 AI 的启动指令

**轮次更新：执行前先阅读第 15 节。2026-09-06 新一轮要求当前仅授权编写指导文档，不授权实施代码修改；须等用户后续明确要求实施。以下通用启动指令不覆盖这一边界。**

> 请先完整阅读项目根目录的《作品集升级执行规范.md》，再检查实际代码、素材和已有进度记录。
>
> 已确认方向是 B 配色（纸白 #FDF9F2、墨黑 #171318、朱红 #E5483B、酸黄 #DDF000）＋海报首屏 A。第一轮三个精选使用现有摄影和绘画。具体选片和蓝色眼睛穿入姓名的构图是待样稿验证的建议，不要误写成已认可成稿。
>
> 请按尚未完成的最早阶段推进；若目前仍未制作样稿，就先完成 P0 与 P1，交付可打开的桌面／手机首页 v0。之后根据反馈推进动效、Works、详情页、About、整站统一和发布验收。不要一次性生成全站或擅自进入可选高阶实验。
>
> 设计判断以我的 Visual DNA 和真实作品为准。web-design-engineer 等技能只作辅助，其 taste 不够贴合时不要套用。保留真实材料、作者性、视觉摩擦和清楚的作品入口，不用模板化组件替代艺术指导。
>
> 保护工作区已有修改。不要编造素材、联系方式、原创角色、成绩或完成状态。完成实现后做相应检查，记录真实结果与下一步，让另一个模型能继续。

---

本文件是完整路线图与交接规范，不是新版网站已经实现的证明。后续以实际文件、可打开页面、验证证据和用户反馈更新执行状态。

## 15. 第二轮：内页阅读节奏与交互收敛（2026-09-06）

### 15.1 状态与范围

**状态：方案已记录，尚未实施。当前用户要求为“指导修改但不操作”，本次仅更新文档。** 后续收到明确的实施请求后，按本节顺序推进；不把写入规范等同于代码完成或视觉认可。

本轮包含五项：内页节奏差异、摄影阅读路径、Sketchbook 整体浏览、首页访客文案、选择反馈与键盘焦点。保留 B 配色的精确色值、已确认的首页海报方向、真实素材及联系入口、现有路由与作品链接。页面差异通过内容顺序、标题尺度、密度和媒体占比形成，不为每页另造一套色板。

不包含：Works 分类结构重做、首页与 Works 精选重选、补拍技术项目截图、摄影年份定稿、新实验功能、部署。上述事项及上一轮未完成验收继续独立记录，不能混入本轮完成声明。

### 15.2 内页拥有不同节奏

目标：访客进入不同媒介页面时，首先遇到与其阅读任务相符的内容，避免反复经过“大标题＋右侧短句＋右上红方块”。

| 页面 | 建议阅读顺序与构图 | 应收敛的元素 | 保留的功能 |
|---|---|---|---|
| About | 紧凑的作者档案：较小的 ABOUT 标题、姓名与定位连续阅读，随后进入实践、教育、工作工具与联系信息 | 过高的标题区、与其他内页重复的装饰方块、无内容作用的空白 | 真实经历与学习状态的区分、邮箱、GitHub、导航 |
| 摄影详情 | 简短书名与说明之后立即出现可翻阅图册；创作文字和元信息放在图册之后 | 重复封面、超大标题门面、图册前多层引导 | 完整作品、原始比例、翻页与返回入口 |
| Sketchbook | 紧凑标题和视图控制后直接展示画面，形成可展开的工作台 | 大标题占满首屏、与 About 相同的左右分栏引言 | 所有原始图片、完整比例、放大查看与返回入口 |
| 放映机 | 缩短标题为机器名称与一句必要操作提示，尽早显示当前画面和控制器 | 三行巨型标题、重复红方块与冗长介绍 | 三种卷轴、指定作品、换帧、项目链接和普通索引出口 |

Works 在本轮保留当前结构，作为上述页面的稳定索引。不得为追求差异任意改变作品次序、删正文或添加未经证实的作者叙事。桌面与手机分别安排内容顺序，不以强制固定首屏高度压缩内容。

验收：四个页面的首段不再重复同一种构图；About 更快进入作者信息，Sketchbook 更快出现图片，摄影更快开始翻阅，放映机更快出现当前画面。导航、联系方式和返回路径保持可达。

### 15.3 摄影详情减少重复入口

建议路径：`返回 Works → 简短书名与说明 → 图册（封面只出现一次）→ 创作文字与元信息 → 返回 Works`。

- 在摄影专用分支中移除独立封面预览区，把现有摘要放入紧凑书名区；其他项目的预览区不随之删除。
- 保留现有 PhotographyFlipbook 与原始素材，不替换翻页引擎。收紧其外部顶部留白及重复标题，避免书名、预览封面、TURN THE BOOK、图册封面成为四次入口。
- 图册中的封面作为唯一阅读起点；前后按钮、当前页数和简短操作提示应邻近画面。触摸、键盘和直接按钮均可完成翻阅。
- 创作正文在图册之后可继续阅读，不默认折叠隐藏。年份冲突不在版式调整时擅自定稿，也不新增未经核对的时间标签。
- 未初始化脚本时须保留可读的基础内容；调整不得破坏原有回退能力。

验收：进入摄影页后不用点击第二个“进入”按钮即可开始翻阅；封面不在图册之外重复出现；图片无强制裁切，翻页、页码、首尾禁用状态及返回链接正常。

### 15.4 Sketchbook 增加整体浏览能力

提供“铺开 / OVERVIEW”和“逐张 / ONE AT A TIME”两种明确视图，默认铺开，包括手机端。沿用同一套素材与排序，避免为两个模式维护重复内容。

- 铺开：桌面默认两列等宽图片，手机一列；保留原比例与完整画面。通过留白和图片自然高度形成节奏，不用强制裁切、任意跨列或极小缩略图换取整齐。
- 逐张：突出单张完整画面，提供可见的上一张、下一张、`当前 / 总数` 和返回铺开按钮；不能只依赖横向滑动或 Hover。首尾采用禁用边界，避免访客不知不觉回到开头。
- 在两种模式间保留最近浏览的图片位置；从放大查看返回后仍能定位原图。视图切换不应把用户突然带回页面顶部。
- 沿用现有放大弹层：Esc 关闭、左右键、触摸路径、关闭后焦点返回触发图片。页面级逐张键盘监听不能与弹层争抢方向键，也不能截获输入控件中的按键。
- 无 JavaScript 时默认铺开仍显示全部图片；不可用的视图切换控件应隐藏或提供诚实的静态回退。空素材目录显示真实空状态。

验收：两种视图均覆盖同一批图片；图片不裁切，320px 页面无意外横向溢出；切换后位置可理解；前后按钮、边界、弹层和焦点返回可实际操作。

### 15.5 首页移除制作过程文案

- 清查访客可见文案中的 `first-round`、`existing material`、样稿轮次与内部制作说明。此类信息移入进度文档，不作为作品介绍。
- 将 “Photography, painted notes, and unfinished visual worlds. These are first-round selections from existing material.” 改为简短的媒介与观看主题说明。
- 可作为编辑起点的候选文案：`Photography and painted notes: traces of looking, remembered places, and imagined figures.` 这是待结合真实选片核对的建议，不是用户确认的创作陈述。
- 不借文案优化虚构系列背景、作品之间的概念关系、成果或作者意图；不改动本轮未授权重选的素材与标题。
- 明确区分内部制作说明与必要的真实性标注：技术项目的截图待补、原型状态仍需如实说明，不能一并清除。

验收：首页读起来是给访客的作品介绍；不出现内部轮次说明；真实待补状态仍透明。

### 15.6 反馈更克制，焦点与选中分离

统一规则：选中说明“正在看什么”，焦点说明“接下来按键会操作什么”，Hover 只提示可操作。

- 普通选择只用约 120–180ms、2–4px 的短暂位移作为建议起点；去掉放映机换帧的对比度闪变和多段抖动。不要把这一收敛范围扩展到已确认的首页眼睛主动作。
- 已选中：保留稳定的填色、文字标记及 `aria-pressed` 等真实语义；不能靠持续动画表达。
- 键盘焦点：增加独立、清晰的 `:focus-visible` 外框或等效标记。在已选中按钮上仍可区分焦点；纸白和墨黑区域分别验证对比度。不得仅使用 `outline: none` 或复制选中背景作为焦点样式。
- 点击随机换帧后，将焦点保留在 CHANGE FRAME 按钮，支持连续 Enter／Space 操作。点击卷轴后优先保留卷轴焦点，点击具体作品后保留该作品按钮焦点，不因画面变化自动跳到内容区。
- 使用简短的礼貌播报宣布当前作品名和位置；不要在每次切换时朗读整段摘要或整个幻灯片列表。
- 检查放映机 `.projector-slide`、`.projector-frame` 的 `display: grid` 是否覆盖原生 `[hidden]`。若复现，增加范围明确的隐藏规则，确保仅当前画面可见、隐藏内容不可聚焦、不可被辅助技术读到。此项属于切换正确性的必要修复，尚不可宣称已修复。
- 减少动态模式直接完成状态切换，仍保留选中、焦点和播报。未来若加入视频，提供播放控制，不因选择作品自动播放。

验收：连续点击或按键换帧不丢失控制器焦点；每次只显示一张幻灯片；选中与焦点可同时辨认；无闪烁，减少动态模式下功能完整。

### 15.7 后续实施顺序与文件范围

仅在用户授权实施之后执行：

1. 检查现有 Git 改动与页面渲染，记录保护项；修复放映机隐藏状态、焦点和反馈，清理首页制作过程文案。
2. 收紧摄影入口与 About 标题区，缩短放映机介绍区；逐页核对阅读顺序。
3. 实现 Sketchbook 双视图、位置保留与弹层协作。
4. 执行本轮验收并更新进度，只标记实际验证通过的事项。

预期涉及文件（执行前需核对实际内容）：

| 范围 | 文件 |
|---|---|
| 首页文案 | `src/components/home/PosterHome.astro` |
| About 节奏 | `src/components/about/AboutArchive.astro`、`src/styles/archive-b.css` |
| 摄影入口 | `src/components/works/ProjectArchive.astro`、`src/components/works/PhotographyFlipbook.astro`、相关页面样式 |
| Sketchbook 双视图 | `src/pages/sketchbook.astro`、`src/styles/archive-b.css` 或独立的 Sketchbook 样式 |
| 放映机与选择反馈 | `src/components/works/UnreliableProjector.astro`、`src/styles/projector-lab.css`；必要时对现有选择器补充局部焦点样式 |

优先局部组件／页面样式，不整份重写 `global.css`；不增加大型动画或画廊依赖。回退应按本轮 diff 撤销相应改动，保留此前用户已有工作，禁止整仓重置。

### 15.8 交付与证据

- 实施后检查 `npm run check`、`npm run build`（均设置 `ASTRO_TELEMETRY_DISABLED=1`）和 `git diff --check`。
- 实际观察 360px、390px、768px、1440px，并检查 320px 最小宽度；重点记录内页开场、摄影图册、Sketchbook 两种视图及放映机状态。
- 验证键盘 Tab／Enter／Space／方向键／Esc、触摸按钮、减少动态模式、200% 缩放和 JavaScript 不可用的回退。
- 在进度文档中分别记录“代码已实现”“浏览器已观察”“交互已实测”和“仍待验收”；构建成功不替代后三者。
- 本轮文档交付不执行上述构建或网站验收，不代表上一轮遗留验收完成，不包含发布。

### 15.9 实施记录（2026-09-06）

本轮已收到实施授权并完成核心代码修改：

- 首页精选区已移除 `first-round selections from existing material`，改为访客可读的媒介与观看主题文案。
- 摄影详情移除了图册前的独立封面预览；摘要进入精简书名区，随后直接进入图册。图册开场改为 `READING MODE`，保留封面作为唯一阅读起点。
- About 收紧了标题区的尺寸和上下留白，移除重复的朱红方块；Sketchbook 收紧标题区，并新增默认 OVERVIEW 与 ONE AT A TIME 两种浏览方式。
- 放映机标题区已缩短；范围内 `[hidden]` 规则确保只显示当前画面与当前卷轴的作品按钮；换帧更新短播报，且不再将焦点跳离 CHANGE FRAME。
- 选择交互使用短暂的 160ms 位移，不再使用对比度闪变或多段抖动；模式与作品按钮有独立的朱红焦点外框，仍保留选中状态填色。

已实测：Sketchbook 两种视图切换、单张首张边界、放大弹层 Esc 后焦点返回；摄影入口内容顺序；放映机仅显示当前画面、更新当前作品且保持 CHANGE FRAME 焦点。尚未完成 360px／390px／768px／1440px 的完整设备级验收、200% 缩放、所有键盘路径、减少动态与 JavaScript 不可用的回退验收；不得将这些项目标为通过。
